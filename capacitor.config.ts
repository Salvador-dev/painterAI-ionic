import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example2.app',
  appName: 'Painter AI',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
